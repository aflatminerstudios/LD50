/// @description Insert description here
// You can write your code in this editor

/*
var boundLeft = objMetaControl.boundLeft + borderX;
var boundRight = objMetaControl.boundRight + borderX
var boundUp = objMetaControl.boundUp + borderY;
var boundDown = objMetaControl.boundDown + borderY;

for (var i = 0; i < numGridX; i++) {
  for (var j = 0; j < numGridY; j++) {
    draw_line_width_color(boundLeft + i * gridWidth, boundUp + j * gridWidth, boundLeft + i * gridWidth, boundUp + (j + 1) * gridWidth, 1, c_yellow, c_yellow);
    draw_line_width_color(boundLeft + i * gridWidth, boundUp + j * gridWidth, boundLeft + (i+1) * gridWidth, boundUp + j * gridWidth, 1, c_yellow, c_yellow);
    draw_line_width_color(boundLeft + (i+1) * gridWidth, boundUp + j * gridWidth, boundLeft + (i+1) * gridWidth, boundUp + (j + 1) * gridWidth, 1, c_yellow, c_yellow);
    draw_line_width_color(boundLeft + i * gridWidth, boundUp + (j+1) * gridWidth, boundLeft + (i+1) * gridWidth, boundUp + (j + 1) * gridWidth, 1, c_yellow, c_yellow);
  }
}
*/
/*
//Draw "wall" on left side
draw_line_width_color(boundLeft + 1, boundUp, boundLeft + 1, boundDown, 3, c_yellow, c_yellow);
*/
